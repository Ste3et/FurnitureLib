/*
 * This file was generated by the Gradle 'init' task.
 */

plugins {
	id "maven-publish"
    id "com.gradleup.shadow" version "9.1.0"
    id "com.modrinth.minotaur" version "2.+"
}

apply plugin: 'de.Ste3et_C0st.furniture.java-conventions'

dependencies {
    implementation project(path: ':FurnitureLib-Core', configuration: 'shadow')
    implementation project(path: ':FurnitureLib-Paper')
}

tasks {
   jar {
	   enabled = true
   }
   
   assemble{
    	dependsOn(shadowJar)
   }
}

def moduleJars = fileTree("$rootDir").matching {
    include "FurnitureLib-Core/build/libs/*.jar"
    include "FurnitureLib-Paper/build/libs/*.jar"
    include "FurnitureLib-1_19_4/build/libs/*.jar"
    include "FurnitureLib-26_2/build/libs/*.jar"
    include "FurnitureLib-Folia/build/libs/*.jar"
    exclude "*-sources.jar"
    exclude "*-javadoc.jar"
}

def foliaJar = file("$rootDir/FurnitureLib-Folia/build/libs")
    .listFiles()
    ?.find { it.name.endsWith(".jar") && !it.name.contains("sources") && !it.name.contains("javadoc") }

def displayFeature = file("$rootDir/FurnitureLib-1_19_4/build/libs")
    .listFiles()
    ?.find { it.name.endsWith(".jar") && !it.name.contains("sources") && !it.name.contains("javadoc") }
    
def currentTag = System.getenv("CI_COMMIT_TAG") ?: "git describe --tags --abbrev=0".execute().text.trim()
def previousTag = "git describe --tags --abbrev=0 ${currentTag}^".execute().text.trim()
def isTag = System.getenv("CI_COMMIT_TAG") != null

tasks.shadowJar {
    if (System.getenv("CI") == null) {
        dependsOn(
            ":FurnitureLib-Core:build",
            ":FurnitureLib-Paper:build",
            ":FurnitureLib-1_19_4:build",
            ":FurnitureLib-26_2:build",
            ":FurnitureLib-Folia:build"
        )
    }
}

shadowJar {
    archiveClassifier.set('')
    archiveVersion.set('')

    mergeServiceFiles()
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE

    dependencies {
        exclude(dependency('com.zaxxer:.*'))
        exclude(dependency('net.kyori:.*'))
    }
    
    zip64 true

    doFirst {
        def jars

        if (System.getenv("CI") != null) {
            println ">>> CI MODE"

            jars = fileTree("$rootDir").matching {
                include "FurnitureLib-*/build/libs/*.jar"
            }.files.findAll {
                it.name.endsWith(".jar") &&
                !it.name.contains("sources") &&
                !it.name.contains("javadoc") &&
                !it.name.contains("FurnitureLib.jar")
            }

        } else {
            println ">>> LOCAL MODE"

            jars = fileTree("$rootDir").matching {
                include "FurnitureLib-*/build/libs/*.jar"
            }.files.findAll {
                it.name.endsWith(".jar") &&
                !it.name.contains("sources") &&
                !it.name.contains("javadoc") &&
                !it.name.contains("FurnitureLib.jar")
            }
        }

        if (jars.isEmpty()) {
            println ">>> WARNING: NO JARS FOUND"
        }

        jars.each {
            println ">>> Including: ${it.absolutePath}"
            from(zipTree(it))
        }
    }
}

def getChangelog = {
	if(isTag){
		def process = ["git", "log", previousTag + ".." + currentTag, "--pretty=- %s"].execute()
	    process.waitFor()
	    return process.text.trim()	
	}else{
		def process = ["git", "log", "HEAD~1..HEAD", "--pretty=- %s"].execute()
	    process.waitFor()
	    return process.text.trim()
	}
}

tasks.modrinth {
    dependsOn = []
}

tasks.publish {
    dependsOn = []
}

modrinth {
    token = System.getenv("MODRINTH_TOKEN")
    projectId = "DqcgUFzu"
    versionNumber = project.version
    if(isTag){
    	versionType = "release"
    } else {
    	versionType = "beta"
    	versionNumber = "${project.version}-dev-${System.getenv("CI_COMMIT_SHORT_SHA")}"
    }
    uploadFile = file("$buildDir/libs/FurnitureLib.jar")
    gameVersions = gradle.mcVersions
    changelog = getChangelog()
    loaders = ["paper", "spigot", "folia"]
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifact(tasks.shadowJar)
            groupId = "de.ste3et"
            artifactId = "furniturelib"
            version = project.version
            artifact(file("$buildDir/libs/FurnitureLib.jar"))
        }
    }

    repositories {
    	mavenLocal()
        maven {
            name = "GitHubPackages"
            url = uri("https://maven.pkg.github.com/Ste3et/FurnitureLib")

            credentials {
                username = "Ste3et"
                password = project.findProperty("gpr.key") ?: System.getenv("GITHUB_TOKEN")
            }
        }
    }
}

subprojects.each {
	subproject -> {
		evaluationDependsOn(subproject.path)
	}
}